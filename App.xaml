<Application x:Class="GameTracker.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!-- Reptilian palette -->
        <SolidColorBrush x:Key="SwampDeep"    Color="#0a1410"/>
        <SolidColorBrush x:Key="SwampMid"     Color="#142016"/>
        <SolidColorBrush x:Key="ScaleDark"    Color="#1c2a1e"/>
        <SolidColorBrush x:Key="ScaleMid"     Color="#243a26"/>
        <SolidColorBrush x:Key="ScaleEdge"    Color="#2e4a30"/>
        <SolidColorBrush x:Key="MossRim"      Color="#4a7c3a"/>
        <SolidColorBrush x:Key="LizardBright" Color="#7cc44a"/>
        <SolidColorBrush x:Key="EyeAmber"     Color="#d4a437"/>
        <SolidColorBrush x:Key="BellyCream"   Color="#e8e0c4"/>
        <SolidColorBrush x:Key="MossText"     Color="#c4d4a8"/>
        <SolidColorBrush x:Key="MossTextDim"  Color="#7a9070"/>
        <SolidColorBrush x:Key="MossTextFade" Color="#506050"/>
        <SolidColorBrush x:Key="VenomRed"     Color="#9a3a2a"/>

        <!-- Snake-scale tile pattern: staggered fish-scale arcs -->
        <DrawingBrush x:Key="ScaleBrush" TileMode="Tile"
                      Viewport="0,0,28,28" ViewportUnits="Absolute" Stretch="None">
            <DrawingBrush.Drawing>
                <DrawingGroup>
                    <GeometryDrawing Brush="#0a1410">
                        <GeometryDrawing.Geometry>
                            <RectangleGeometry Rect="0,0,28,28"/>
                        </GeometryDrawing.Geometry>
                    </GeometryDrawing>
                    <GeometryDrawing>
                        <GeometryDrawing.Pen>
                            <Pen Brush="#1c2a1e" Thickness="1"/>
                        </GeometryDrawing.Pen>
                        <GeometryDrawing.Geometry>
                            <PathGeometry>
                                <PathFigure StartPoint="0,8">
                                    <ArcSegment Point="14,8" Size="7,7" SweepDirection="Clockwise"/>
                                </PathFigure>
                                <PathFigure StartPoint="14,8">
                                    <ArcSegment Point="28,8" Size="7,7" SweepDirection="Clockwise"/>
                                </PathFigure>
                                <PathFigure StartPoint="-7,22">
                                    <ArcSegment Point="7,22" Size="7,7" SweepDirection="Clockwise"/>
                                </PathFigure>
                                <PathFigure StartPoint="7,22">
                                    <ArcSegment Point="21,22" Size="7,7" SweepDirection="Clockwise"/>
                                </PathFigure>
                                <PathFigure StartPoint="21,22">
                                    <ArcSegment Point="35,22" Size="7,7" SweepDirection="Clockwise"/>
                                </PathFigure>
                            </PathGeometry>
                        </GeometryDrawing.Geometry>
                    </GeometryDrawing>
                </DrawingGroup>
            </DrawingBrush.Drawing>
        </DrawingBrush>

        <!-- Custom title bar controls -->
        <Style x:Key="WindowControlButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#7a9070"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="44"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontFamily" Value="Segoe UI Symbol"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1c2a1e"/>
                                <Setter Property="Foreground" Value="#7cc44a"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="WindowCloseButton" TargetType="Button" BasedOn="{StaticResource WindowControlButton}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#9a3a2a"/>
                                <Setter Property="Foreground" Value="#e8e0c4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>
